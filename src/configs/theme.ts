export const colors = {
  white: "#ffffff",
  black: "#333333",
  darkGray: "#424242",
};

export const shadow = {
  grayShadow: "0px 0px 50px rgba(0, 0, 0, 0.3)",
};

const theme = {
  colors,
  shadow,
};

export default theme;
